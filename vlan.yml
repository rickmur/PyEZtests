---
VlanTable:
  rpc: get-vlan-information
  item: vlan
  view: VlanView
  key: vlan-index

VlanView:
  fields:
    instance: vlan-instance
    vlan_name: vlan-name
    created: vlan-create-time
    status: vlan-status
    owner: vlan-owner
    tag: vlan-tag
    members: .//vlan-member-interface
